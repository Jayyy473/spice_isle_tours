# Ensure index.php is served by default
DirectoryIndex index.php index.html

# Allow URL rewriting (optional but recommended)
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /
</IfModule>

# Fix permission issues
<IfModule mod_authz_core.c>
    Require all granted
</IfModule>
